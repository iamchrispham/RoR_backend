class ShowoffSnsCreateNotifiable<%= table_name.camelize %> < ActiveRecord::Migration<%= migration_version %>
  def change
    create_table :<%= table_name %> do |t|
  <% attributes.each do |attribute| -%>
    t.<%= attribute.type %> :<%= attribute.name %>
  <% end -%>
      t.boolean :notifications_enabled, :boolean, default: true

      t.timestamps null: false
    end
  end
end
