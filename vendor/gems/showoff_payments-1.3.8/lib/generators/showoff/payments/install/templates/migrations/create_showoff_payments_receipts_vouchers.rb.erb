class CreateShowoffPaymentsReceiptsVouchers < ActiveRecord::Migration
  def change
    create_table :showoff_payments_receipts_vouchers do |t|
      t.belongs_to :receipt

      t.integer :voucher_id, null: false, index: true
      t.string :voucher_type, null: false, index: true
    end

    add_index :showoff_payments_receipts_vouchers, [:voucher_id, :voucher_type], name: :showoff_payments_receipts_vouchers_voucher
  end
end
