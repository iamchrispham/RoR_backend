Description:
    This will install the migrations necessary to managed accoutn columns to vendor identities

Example:
    rails generate showoff:payments:add_managed_accounts_to_vendor_identities

    This will create:
        db/migrate/add_managed_accounts_to_vendor_identities.rb
