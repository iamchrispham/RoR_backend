<p><%= t("devise.mailer.invitation_instructions.hello", email: @resource.name) %></p>

<p>Someone has invited you to be an admin on Go!</p>

<p>
  <%= link_to accept_invitation_url(@resource, :invitation_token => @token), class:"center-block email-button button-custom"  do %>
      <span class="white"><%= t("devise.mailer.invitation_instructions.accept") %></span>
  <% end %>
</p>

<% if @resource.invitation_due_at %>
  <p><%= t("devise.mailer.invitation_instructions.accept_until", due_date: l(@resource.invitation_due_at, format: :'devise.mailer.invitation_instructions.accept_until_format')) %></p>
<% end %>

<p><%= t("devise.mailer.invitation_instructions.ignore").html_safe %></p>
